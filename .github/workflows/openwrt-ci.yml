name: Build LEDE (Stable ISO/QCOW2)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    # --- 修复核心：清理空间 + 挂载 Swap ---
    - name: Free Disk Space (Maximize)
      uses: jlumbroso/free-disk-space@main
      with:
        # 移除不需要的大文件，释放约 10-12GB 空间
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: false

    - name: Initialize Environment & Create Swap
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        # 1. 创建 8GB SWAP 虚拟内存 (防止 OOM 内存溢出导致 Runner 断连)
        echo "Creating SWAP file..."
        sudo fallocate -l 8G /swapfile
        sudo chmod 600 /swapfile
        sudo mkswap /swapfile
        sudo swapon /swapfile
        echo "Swap info:"
        free -h

        # 2. 安装编译依赖
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install $(curl -fsSL git.io/depends-ubuntu-2204)
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$USER /workdir

    - name: Clone source code
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: Load custom feeds
      run: |
        cd openwrt
        echo "src-git passwall_packages https://github.com/xiaorouji/openwrt-passwall-packages.git;main" >> "feeds.conf.default"
        echo "src-git passwall https://github.com/xiaorouji/openwrt-passwall.git;main" >> "feeds.conf.default"
        
        mkdir -p package/custom
        git clone --depth 1 https://github.com/vernesong/OpenClash.git package/custom/luci-app-openclash

    - name: Update feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load custom configuration
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        
        cd openwrt
        if [ ! -f .config ]; then
          make defconfig
        fi

        # 开启插件
        sed -i 's/# CONFIG_PACKAGE_luci-app-passwall is not set/CONFIG_PACKAGE_luci-app-passwall=y/' .config
        sed -i 's/# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray is not set/CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y/' .config
        sed -i 's/# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox is not set/CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=y/' .config
        sed -i 's/# CONFIG_PACKAGE_luci-app-openclash is not set/CONFIG_PACKAGE_luci-app-openclash=y/' .config

        # 开启镜像格式 (ISO & PVE)
        sed -i 's/# CONFIG_ISO_IMAGES is not set/CONFIG_ISO_IMAGES=y/' .config
        sed -i 's/# CONFIG_TARGET_IMAGES_QCOW2 is not set/CONFIG_TARGET_IMAGES_QCOW2=y/' .config
        sed -i 's/# CONFIG_TARGET_IMAGES_GZIP is not set/CONFIG_TARGET_IMAGES_GZIP=y/' .config
        
        # 调整分区大小 (防止空间不足)
        sed -i 's/CONFIG_TARGET_KERNEL_PARTSIZE=16/CONFIG_TARGET_KERNEL_PARTSIZE=64/' .config
        sed -i 's/CONFIG_TARGET_ROOTFS_PARTSIZE=160/CONFIG_TARGET_ROOTFS_PARTSIZE=1024/' .config

        make defconfig

    - name: Download package
      id: package
      run: |
        cd openwrt
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    # --- 修复核心：优化编译参数 ---
    - name: Compile the firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        
        # 第一次尝试：多线程编译
        # 如果失败，自动切换到单线程编译 (make -j1)
        # 单线程虽然慢，但是最省内存，能极大减少崩溃概率
        make -j$(nproc) || make -j1 || make -j1 V=s
        
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Organize files
      id: organize
      if: steps.compile.outputs.status == 'success' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        
        mkdir -p $GITHUB_WORKSPACE/uploads
        mv *.iso $GITHUB_WORKSPACE/uploads/ 2>/dev/null || true
        mv *.qcow2 $GITHUB_WORKSPACE/uploads/ 2>/dev/null || true
        mv *.img.gz $GITHUB_WORKSPACE/uploads/ 2>/dev/null || true
        mv *.config $GITHUB_WORKSPACE/uploads/ 2>/dev/null || true
        
        echo "FIRMWARE=$GITHUB_WORKSPACE/uploads" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware directory
      uses: actions/upload-artifact@v4
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_Firmware_ISO_PVE
        path: ${{ env.FIRMWARE }}

    - name: Delete workflow runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 1
        keep_minimum_runs: 3
